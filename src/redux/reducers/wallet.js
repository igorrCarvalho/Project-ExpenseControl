const walletInitState = {
  currencies: [],
  expenses: [],
  editor: false,
  idToEdit: 0,
};

const wallet = (state = walletInitState, action) => {
  switch (action.type) {
  default:
    return state;
  }
};

export default wallet;
